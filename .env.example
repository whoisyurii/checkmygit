# GitHub API (optional but recommended for full features)
# Create a token at: https://github.com/settings/tokens
# Required scopes: read:user (public_repo optional for more data)
GITHUB_TOKEN=ghp_your_token_here
